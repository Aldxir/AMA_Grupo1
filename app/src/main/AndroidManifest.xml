<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Alden"
        android:name=".AldenApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        </activity>

        <activity
            android:name=".ui.registro.RegistroActivity"
            android:exported="false" />

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".SessionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.rating.RatingActivity"
            android:exported="false"
            android:label="Calificar Experiencia"
            android:parentActivityName=".ui.registro.RegistroActivity"/>

        <service
            android:name=".firebase.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="default_channel_id" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification_default" />
        <activity
            android:name=".ui.rating.AdminRatingsActivity"
            android:exported="false"
            android:label="Reporte de Ratings"
            android:parentActivityName=".ui.registro.RegistroActivity"/>

        <activity
            android:name=".charts.ui.ChartsActivity"
            android:exported="false"
            android:label="Estadísticas"
            android:parentActivityName=".ui.registro.RegistroActivity" />

    </application>

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

</manifest>